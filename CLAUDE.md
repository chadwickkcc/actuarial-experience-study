# Experience Study Tool — Project Briefing

## What this project is
A Python-based actuarial experience study and TEV modelling tool for life insurance.
It covers 5 products: Term Life, Whole Life, Universal Life (UL/ULSG), Variable
Universal Life (VUL), and Deferred Annuities.

## Governing documents (read these before writing any code)
**Latest authoritative specs (supersede the older v2.1 / v1.2 still in /docs):**
- @docs/experience_study_requirements_spec_v3_0_1.md — full functional requirements (incl. Phase 3
  AI layer, FR-3A/FR-3B series), phase map, synthetic data spec, and non-functional requirements
- @docs/experience_study_technical_spec_v2_0_1.md — exact DuckDB DDL (Section A; §D for AI Gold
  tables), Python module interface contracts (Section B; §E for AI modules), synthetic data generator
  spec (Section C), config schemas (§F)

**Phase 3 build driver + progress (read before any Phase 3 session):**
- @docs/phase3_claude_code_prompts.md — the per-session prompt blocks (Sessions 14–22)
- @docs/phase3_build_progress.md — what is built so far, DoD status, known limitations, next session

## Key rules
1. All database schemas must exactly match the DDL in the technical spec (v2.0.1 Section A;
   §D for the new AI Gold tables). Do not add, rename, or remove columns from those schemas.
2. All module function signatures must exactly match the interfaces in the technical spec
   (v2.0.1 Section B; §E for the AI layer). Do not invent alternative return types.
3. All product-specific logic goes in YAML config files under config/products/.
   The calculation engine must be product-agnostic Python.
4. The random seed for all synthetic data generation is 42.
5. Every function must have a docstring. PEP 8 style throughout.
6. DuckDB file lives at data/experience_study.duckdb.
7. Do not use SQLAlchemy or any ORM. All DB access uses the duckdb Python package directly.
8. (Phase 3) The AI layer is strictly additive under src/ai/: the core engine never imports it;
   it reads only the Gold layer + config, and writes only to data/ai_models/ and the three AI Gold
   tables. ALL dynamically-built SQL in the AI layer goes through src/utils/sql_boundary.py — no
   string-interpolated SQL anywhere in src/ai/. Every threshold/seed/grain lives in config YAML.

## Build phases (do not mix phases in one session)
- Phase 1A: Term Life (foundation + full MVP pipeline) — DONE
- Phase 1B: Whole Life + UL/ULSG (extend existing pipeline) — DONE
- Phase 1C: VUL + Deferred Annuities (extend existing pipeline) — DONE
- Phase 2: TEV Modelling — DONE
- Phase 3: AI Layer — **DONE & SIGNED OFF (Phase 3 CLOSED 2026-06-28)** (build complete; the owner ran the live eval baseline ≥2 models + the manual UI adversarial prompts offline, and **accepted Phase 3 UAT on 2026-06-28** — sign-off recorded in `docs/phase3_uat_script.md` / `docs/phase3_uat_runbook.md`; offline regression gate **1163 passed, 6 skipped**, no keys)
  - Phase 3a (Sessions 14–17): security hardening, GLM, GBM+SHAP, Assumption Comparison UI
  - Phase 3b (Sessions 18–22): LLM abstraction, MCP server, Skills, chatbot, eval harness
  - **Sessions 14 (security hardening), 15 (GLM Assumption Engine), 16 (GBM Overlay + SHAP) and 17 (Assumption Comparison UI) COMPLETE — Phase 3a CLOSED.**
  - **Session 18 (LLM Provider Abstraction + MCP Server) COMPLETE — Phase 3b STARTED (2026-06-20).** Phase 0 done: interpreter moved off 3.9.6 to **Python 3.12 in a uv-managed `.venv`** (the just-installed system 3.14 has no wheels for the numba/llvmlite→shap stack); `anthropic`/`openai`/`mcp` added to `requirements.in`, lockfile regenerated for 3.12 with **no ML-stack pin drift**, baseline re-verified. Built: `src/ai/llm/` (provider abstraction + MockProvider), `src/ai/mcp_server/server.py` (5 read-only tools, stdio, server-side gates), the two remaining AI Gold tables, `config/llm_config.yaml`. Suite **874 passed, 6 skipped** (no keys; post-audit). Model pricing in `llm_config.yaml` filled 2026-06-20 from public rates (Anthropic reference for Opus 4.8 $5/$25 and Sonnet 4.6 $3/$15; DeepSeek V4 GA rates Pro $0.435/$0.87, Flash $0.14/$0.28) — owner-overridable. Post-build audit (2026-06-20) fixed one latent bug — `AnthropicProvider` no longer forwards `temperature` (Opus 4.7+/Fable reject sampling params with a 400) — and added +17 tests (provider SDK mapping/retry/missing-SDK; MCP end-to-end through FastMCP + JSON-serializability).
  - **Session 19 (Claude Skills) COMPLETE (2026-06-20).** Built the two prompt-artifact Skills — `interpret_ae_and_draft_memo` (eight-component A/E memo) and `explain_shap_results` (SHAP narrative) under `src/ai/skills/`, each running on any configured model via the provider abstraction and **blocking, never repairing** on a failed numeric post-check (FR-3B-19/22). Pulled `verify_traceability` + `TraceabilityResult` forward into `src/ai/chatbot/traceability.py` (the canonical §E.7 checker — Session 20 consumes it unchanged). Added the `config/prompts/` versioned-template loader/hasher (`src/ai/prompts.py`) + `config/prompts/skills/{memo,shap_explain}.md`, a `skills:` call-params block in `llm_config.yaml`, and `ui/skills_logic.py`; un-greyed the two Skill buttons on the Assumption Comparison page and added the memo Skill to Stage-4 governance (FR-3B-20). Post-build audit (2026-06-20) fixed two correctness defects — UUID digit-leakage into the traceability allowed-set (run_id/model_id now excluded) and untranslated SHAP grain-key feature names reaching the LLM (FR-3B-22) — and added +19 tests. Suite **911 passed, 6 skipped** (no keys; +37 tests, no regressions).
  - **Session 20 (Chatbot Core + Guardrails) COMPLETE (2026-06-20).** Built the rest of `src/ai/chatbot/` around the existing `traceability.py`: `session.py` (`SessionState` + cost helper), `context.py` (`trim_history` + RAG stub), `mcp_client.py` (`InProcessMCPClient` — the only DB path, FR-3B-25) and `pipeline.py` (the seven stages + `handle_turn`). All five SQL gates delegate to `sql_boundary.validate_select` and are re-enforced server-side by the MCP tools; the mandatory `verify_traceability` post-check **blocks, never repairs** (FR-3B-34); numbers are filled programmatically by `fill_numeric_slots` (fixed `{{col:..}}`/`{{agg:fn:..}}` grammar) — the LLM never emits a value (FR-3B-33). Intent is logged before any data access (FR-3B-27); OUT_OF_SCOPE / write / assumption-change → templated refusal (FR-3B-42); multi-turn trim keeps the system prompt (FR-3B-39); max-turns (FR-3B-40) and budget warn-80%/stop-100% (FR-3B-44); model switchable mid-session (FR-3B-45). §E.7 reconciliation (owner-confirmed): the SQL-gen call returns JSON `{sql, answer_template}`, so `generate_sql(...) -> str` stays a thin wrapper. Authored `config/prompts/{routing,sql_generation}.md` + `config/chatbot_few_shots.yaml`; added multi-turn/budget keys to `config/ai_config.yaml`; added `IntentLabel`/`ChatTurnResult` to `src/utils/types.py`. Post-build correctness audit (2026-06-20) added +17 tests (TEV data path; routing/`_parse_plan` robustness; cost accounting from `llm_config` prices; model-switch honored on the data path; empty-result + negative-number handling; the **cross-table UNION injection vector** caught at routing; a no-direct-DB-connection guard over the chatbot core) — no code defects found, behaviour confirmed. Suite **962 passed, 6 skipped** (no keys; +51 Session-20 tests, no regressions).
  - **Session 21 (RAG Commentary + Audit + AI Analyst Page) COMPLETE (2026-06-20).** Filled the four Session-20 seams: `context.assemble_rag_context`/`resolve_rag_artifacts` ground commentary in the tool's **own** artifacts (Working/Chief Actuary reports + shipped methodology docs, never an external KB, FR-3B-36); the `COMMENTARY_GENERATION` route now drafts via the data path (commentary.md → `{sql, answer_template}` → validate → MCP execute → slot-fill → `verify_traceability` block-not-repair, FR-3B-37) carrying the persistent "AI-drafted — pending actuary review" banner that survives export (FR-3B-38); the optional LLM-as-judge faithfulness score (off by default) **flags, never blocks** and is logged (FR-3B-46); every turn writes the full §D.3 field set to `gold_ai_audit_log` via a static parameterized INSERT (`src/ai/chatbot/audit.py`, FR-3B-47), queryable from the Study Run Log page (NFR-A-07). Built the **AI Analyst** page (`ui/pages/16_ai_analyst.py` + `ui/ai_analyst_logic.py`, FR-3B-43): model dropdown (greys missing-key models), run selector scoping data + RAG, running token/cost + budget warn/stop, mid-session model switch (logged), Markdown export with banners. `handle_turn` is now an audit-emitting wrapper over `_run_turn`; the pipeline stays DB-free (FR-3B-25) — the audit sink is the only write. Authored `config/prompts/{commentary,faithfulness_judge}.md` + the `chatbot.rag` config block. Post-build correctness audit (2026-06-20) added +11 tests (audit INSERT column-alignment lock + full-row roundtrip; commentary SQL-gate/slot-fill/generation-failure blocks; faithfulness unparseable→None; None-intent/refusal/budget-stop audit rows; public-wrapper unit tests) — no code defects found. Suite **998 passed, 6 skipped** (no keys; +36 over the 962 baseline, no regressions).
  - **Session 22 (Evaluation Harness + Phase 3 UAT) COMPLETE — Phase 3b / Phase 3 CLOSED (2026-06-20).** Built the CLI eval harness under `src/ai/eval/`: `result_match.py` (`results_match`, the FR-3B-51 column-set/row-count/sorted-multiset/1e-6-tolerance/NULL rule, honouring `value_check`), `runner.py` (`EvalMetrics` + `run_eval` driving the **existing** `handle_turn` per golden question and the gated MCP path for both reference and generated SQL — the two hard gates `gate_integrity`/`numeric_traceability` plus reported execution/routing/refusal; static `?`-placeholder INSERT to `gold_ai_eval_results`, FR-3B-52), and `__main__.py` (`python -m src.ai.eval`: `--models`/`--smoke`, up-front cost estimate + interactive confirm above `eval.eval_cost_confirm_threshold`, per-model table, non-zero exit on a hard-gate fail, and a guard that **refuses to run inside pytest**, FR-3B-53). Authored the locked-pending sets `tests/eval/golden_set.yaml` (36 Q→SQL across all five product families + the five query classes) and `tests/eval/adversarial_set.yaml` (12 injection/write/off-allowlist/PII/out-of-scope/assumption-change probes), the `eval:` block in `ai_config.yaml`, and finalised `docs/phase3_uat_script.md`. Nothing rebuilt — the pipeline, MCP server, `verify_traceability` and SQL gates are reused as-is; no `src/ai/` string-interpolated SQL (the INSERT mirrors `audit.py`/`registry.py`). **Owner lock RESOLVED (2026-06-20):** the owner reviewed and **locked** the golden/adversarial sets as the authoritative baseline (headers record the lock); the live eval baseline (≥2 models) + UAT sign-off are the remaining owner-triggered steps (not part of the pytest gate). Post-build correctness audit (2026-06-20) fixed one spec-compliance defect — `EvalMetrics` trimmed to the exact §E.9 seven-field contract (persistence-only `n_golden`/`n_adversarial`/`actual_cost_usd` moved to `persist_eval_metrics` params) — and added +6 tests (eval-INSERT column-alignment lock + roundtrip; all 36 locked golden queries validated against the real Gold schema; locked adversarial set scored end-to-end; results_match on reordered real rows; CLI smoke wiring + a manual `python -m src.ai.eval --help`/no-keys smoke). Suite **1042 passed, 6 skipped** (no keys; +44 Session-22 tests, no regressions).
  - **Post-UAT hardening (2026-06-25/26):** owner UAT of the two Skills produced bug fixes + one additive feature, all with regression tests; the offline gate stayed green. Highlights: empty-LLM-response now fails loudly (DeepSeek provider + skill guards + higher skill `max_tokens`) instead of a tag+footer-only memo; the §E.7 numeric-traceability checker no longer false-blocks on age/duration bands and dates (`_NUMBER_RE` look-behind so a hyphen between digits is a range separator, not a minus sign); memo prompt → v1.2 (+`study_years`); `_ae_by_segment` now **aggregates** A/E by segment (the all-0.0/"25-29" memo was an assembly artifact — the study data was always correct); SHAP numbers rounded for display; **SURRENDER** added to `DecrementType` as a **memo/experience-only** decrement (guarded out of GLM/GBM — see the §E.1 spec note in the progress doc); IUL lapse `expected_lapses=0` fixed via `_LAPSE_PARENT` `IUL→UL` in `ae_engine.py` (takes effect on the next study run). **Suite 1056 passed, 6 skipped.** Full detail in `docs/phase3_build_progress.md` → "Post-UAT hardening".
  - **Post-UAT hardening (round 2) — AI Analyst chatbot (2026-06-26):** owner UAT of the Session-21 AI Analyst found it unfit (quoted 0 for every A/E, "products = DA_FIA", over-refused follow-ups, DeepSeek routing errored, commentary/faithfulness undiscoverable) — all **query-grain/slot/routing bugs, not bad data** (true WL mortality A/E ≈ 0.5718). Fixed within the governed design: SQL-gen prompt → v1.1 + 6 few-shots teach **A/E = SUM(actual)/SUM(expected)** (`illness_code IS NULL` for mortality/lapse/surrender); a `{{list:}}` slot enumerates multi-row answers; `assemble_response` adds context only for single-row results; routing → v1.1 treats follow-ups as data questions and sees recent history; per-call token caps moved to `ai_config.yaml` `chatbot.max_tokens` and raised (fixes the DeepSeek empty-routing `llm_error`); run scoping injects `study_run_id` into generated SQL; the AI Analyst page gains example/commentary buttons + a faithfulness toggle. **Suite 1070 passed, 6 skipped.** Detail: progress doc → "Post-UAT hardening (round 2)". Live AI Analyst re-test is owner-triggered.
  - **Post-UAT hardening (round 3) — AI Analyst reliability + reasoning (2026-06-26):** round-3 UAT showed commentary still failing (DeepSeek **silence**, Claude "couldn't answer safely") and terse answers. Root causes: SQL-gen/commentary-gen provider errors were **unwrapped** (a truncated reasoning reply crashed the turn → silent UI), and commentary's single-SQL+slot-fill design couldn't fit a real narrative. Owner-approved fixes: **(A)** wrap generation provider errors → safe block + raise `chatbot.max_tokens` (commentary 4096/sql 2048); **(B)** opt-in **Analyst mode** (default OFF) — flag-not-block for numeric traceability, SQL gates never relax; **(C)** **commentary rebuilt on the memo Skill's fact-pack pattern** — `ui/skills_logic.assemble_commentary_facts` (overall+by-segment A/E, **aggregate** credibility, exposure, TEV) → prose generate-then-verify (`commentary.md` v2.0), fixing the silence/"couldn't answer" and the wrongly-averaged "0.0003" credibility; **(D)** **multi-query** EXPLORATORY synthesis (plan→fetch→synthesise, `synthesis_{plan,answer}.md`), feature-flagged `chatbot.multi_query_default: false` (eval/tests on single-query) and ON in the AI Analyst page. `handle_turn` gained `commentary_facts`/`analyst_mode`/`multi_query` (default-safe); pipeline stays DB-free (fact pack assembled in `ui/`). **Suite 1082 passed, 6 skipped.** Detail: progress doc → "Post-UAT hardening (round 3)". Live AI Analyst re-test is owner-triggered.
  - **Robustness hardening (2026-06-27):** +32 offline edge-case tests across number-computation / LLM-interaction / output-assembly (`tests/test_chatbot_robustness_{numbers,llm,output}.py` + 2 real-data fact-pack tests) — slot NULL/agg/list edges, the synthesis "no invented cross-query total" guard, model-switch on every path, parser fail-safety, audit/export shape. No source defect found. **Suite 1114 passed, 6 skipped.** NOTE: three round-2/3 changes (Analyst mode vs FR-3B-34; fact-pack commentary vs FR-3B-37/§E.7; multi-query synthesis) were **formally amended into the locked specs in-place** on owner authorization (2026-06-27 — dated header notes on Req v3.0.1 + Tech v2.0.1, filenames retained; `docs/DEFERRED_FOLLOWUPS.md` FU-5 RESOLVED).
  - **Post-UAT hardening (round 4) — AI Analyst "smarter / know all the data" (2026-06-27):** owner-authorised **governed-maximum** build (raw-chat-like reasoning + breadth **without** breaching the spine — no PII to an LLM, no unflagged invented numbers, no raw DB/SQL, no writes). (A) **Data-surface widening:** the shared Gold allowlist gains the omitted A/E (amount-basis, SE/CI bounds, credibility-weighted, anti-selection) + TEV margin columns and **six PII-free tables** (`gold_inforce_reconciliation`, `gold_dq_run_summary`, `gold_model_points`, `gold_ai_model_registry`, `gold_assumption_sets`, new `gold_ai_proposed_factors`), served by a sixth generic gated **`query_results(table, sql)`** MCP tool (`TOOL_SCHEMA_VERSION`→"2.0"); a **PII-reachability guard test** holds the bright line (no policy_id/Silver/Bronze/person-ids). (B) **Materialised proposals:** `src/ai/proposals.py` writes published GLM/GBM `FactorCell`s to `gold_ai_proposed_factors` (the *fourth* permitted AI Gold write target) at registration, so the analyst can answer "proposed Term mortality assumptions by age band". (C) **Study digest every turn:** `pipeline._render_digest` injects a compact overall-A/E-by-product/decrement digest into routing/SQL-gen/synthesis prompts + the traceable allowed-set (default-safe `study_digest` kwarg; eval/tests inert). (D) **Prompts:** `routing.md`→v1.2 (read≠change), `commentary.md`→v2.1 (cite aggregate credibility, no invented "mean credibility"), `sql_generation.md`→v1.2 (CI legend, ranking, new schema cards), +10 few-shots. (E) **Analyst mode default ON on the AI Analyst page only** (global default stays OFF → eval keeps its 100% numeric-traceability gate). Specs amended in-place (Req v3.0.1 + Tech v2.0.1, round-4 header notes; FR-3B-09 tool count, FR-3B-13/32 allowlist, FR-3A-09 4th write target, §E.7 digest seam). **Suite 1139 passed, 6 skipped** (+25 targeted tests: `test_chatbot_digest`/`test_data_surface`/`test_proposals_integration`, incl. the PII-reachability guard, the register→materialize wiring, and the digest-does-not-defeat-traceability safety lock). Locked golden/adversarial eval sets unchanged. **Re-run note:** re-`init_database` the live DB to create `gold_ai_proposed_factors`, and re-fit AI models on the Assumption Comparison page to populate it; live AI Analyst re-test is owner-triggered. Detail: progress doc → "Post-UAT hardening (round 4)".
  - **Post-UAT hardening (round 5) — AI Analyst output evaluation + formatting fixes (2026-06-27):** owner uploaded four live AI Analyst transcripts (+memos/SHAP) for evaluation. **Figures verified 100% correct** (80+ revalidated against live run `ed697c0c…`); the issues were answer formatting + a few unanswerable cases, not data. Fixes (all within the governed spine): (1) new **`{{table:<cols>}}` slot** in `pipeline._resolve_slots` renders multi-row results as a real markdown table from the result set (numbers traceable) — fixes both the "couldn't answer safely" table failures and the malformed `{{list:}}`-per-column comma-collapsed tables; `sql_generation.md` teaches a single `{{table:}}` per table request. (2) **Commentary fact pack** (`ui/skills_logic.assemble_commentary_facts`) now includes the published GLM **proposed factors** (from `gold_ai_proposed_factors`, with CI/credibility + a `low_credibility` flag), so "commentary on the proposed assumptions" works (`commentary.md`→v2.2). (3) **Friendlier non-security block messages** (slot-fill/no-evidence/commentary give actionable hints; SQL-gate + numeric-traceability stay generic). (4) Minor: non-blocking `run_scope` audit event; A/E-memo "principal drivers" excludes zero-credibility bands; `shap_explain.md`→v1.2 (directional wording). Specs amended in-place (Req v3.0.1 + Tech v2.0.1 round-5 header notes; FR-3B-33 table slot, FR-3B-37 commentary proposed factors, FR-3B-42 hints). No change to SQL gates, MCP-only path, numeric-traceability default, audit schema, or locked eval sets. **Suite 1149 passed, 6 skipped** (+10 tests). Live AI Analyst re-test owner-triggered. Detail: progress doc → "Post-UAT hardening (round 5)".
  - **Post-UAT hardening (round 6) — AI Analyst transcript evaluation + fixes (2026-06-27):** owner ran the 20 suggested test questions across all four models and uploaded four transcripts. **Figures verified 100% correct** against live run `5df3befb…` (all 12 distinct claims exact; no fabrication; PII/assumption-change correctly refused). Defects were refusals/formatting, not accuracy: (1) **🔴 wrong appended credibility** — `assemble_response` read a stored per-cell `credibility_z*` from `row[0]` of an aggregate (UL-lapse showed 0.0015 not 0.3881); now **recomputes Z from the aggregate actual-count** via `ae_engine.compute_credibility_z` (FR-1A-24), traceable, falls back to the stored value only when no count column. (2) **🔴 two wrongful refusals** ("most credible across products"; "largest PVFP profit-source margin") — `routing.md`→v1.3 (superlatives/rankings/credibility/TEV-margin/multi-part are EXPLORATORY) + a bounded **re-route retry** so a token-capped routing reply isn't silently defaulted to OUT_OF_SCOPE + `chatbot.max_tokens.routing` 1024→2048 + 4 few-shots. (3) **🟡 recon+DQ multi-part** — `synthesis_plan.md`→v1.1 adds recon/DQ tables so EXPLORATORY synthesis answers both halves. (4) **🟡 degenerate GLM factors** — `sql_generation.md`→v1.3 caveats near-zero-credibility/exploding-CI proposed cells. Non-chatbot observations (faithful answers): in-force reconciliation only populated for DA; CI per-illness exposure stored as total/10. Specs amended in-place (Req v3.0.1 + Tech v2.0.1 round-6 header notes). No change to SQL gates, MCP-only path, numeric-traceability default, audit schema, or locked eval sets. **Suite 1160 passed, 6 skipped** (+11 tests). **Live re-test follow-up (deepseek-v4-pro, 2026-06-28):** #12/#14/#15/#16 verified correct; #2's *appended* credibility was right (0.3881) but the model still wrote `AVG(credibility_z_lapse)≈0.0015` in its own prose → `sql_generation.md` **v1.4** forbids selecting/averaging a per-cell `credibility_z*` for a roll-up (credibility left to the system-appended aggregate Z) + 1 few-shot, **plus a deterministic backstop** (`pipeline.aggregates_per_cell_stat`) that blocks/skips any generated SQL aggregating a `credibility_z*`/`se_ae*` column regardless of model compliance. **Suite 1163 passed, 6 skipped.** Live AI Analyst re-test owner-triggered. Detail: progress doc → "Post-UAT hardening (round 6)".
  - **Run tests with the venv:** `unset ANTHROPIC_API_KEY DEEPSEEK_API_KEY OPENAI_API_KEY && .venv/bin/python -m pytest tests/ -v --tb=short`. Build/progress detail in `docs/phase3_build_progress.md`.
- Phase 4: Governance (single-org) — **BUILD COMPLETE (Sessions 23–27) + post-S27 governance UI (2026-07-04); owner UAT §8.8 walkthrough + sign-off is the remaining close.** Locked design: `experience_study_requirements_spec_v4_0.md` §8 (FR-4-01..27), `experience_study_technical_spec_v3_0.md` §G/H/I, `phase4_claude_code_prompts.md` (Sessions 23–27), `phase4_locked_scope.md`. Progress: `docs/phase4_build_progress.md`; UAT script: `docs/phase4_uat_script.md`.
  - **Session 23 (Identity & Access Foundation) COMPLETE (2026-06-29).** Added `gold_users` (§G.1), the §H.1 governance types (`Role`/`User`/`ChainLevel`/`ArtifactType`/`Decision`/`SignoffRecord`), `src/governance/{auth,users,rbac}.py` (salted-PBKDF2 **stdlib** hashing — no lockfile change; server-side RBAC with `require()`+denial logging; `may_sign_off_at`), the login gate in `ui/app.py`, and `config/governance_config.yaml` (four §I.2 default seed users + placeholder passwords; real creds via git-ignored `config/governance_config.local.yaml`). Governance is additive, **outside `src/ai/`**, and uses the standard parameterized write path (NOT `sql_boundary`). **26 new governance tests + 8 architecture guards pass; 0 regressions** (suite excluding prod-DB-dependent files: **908 passed, 47 skipped, 0 failed**).
  - **Session 24 (Versioning & Lineage) COMPLETE (2026-06-29).** Added `src/governance/lineage.py` (§H.5): `create_version` (clone-from-parent → DRAFT, or seed-root), `lineage_root`, `approve_and_supersede` (non-overlapping range check → `OverlappingEffectiveRange`; ≤1 APPROVED-current per lineage with supersession), `resolve_live_set`, `compare_versions` (cell diff + ΔTEV + rationale), `reproducibility_stamp`. Added the §G.4 columns (`parent_set_id`/`effective_from`/`effective_to`) to `_COLUMN_MIGRATIONS`, `DRAFT` to `AssumptionSetStatus`, the `VersionDiff` type, and extended `_insert_assumption_set_metadata`'s preserve-loop so a plain re-save keeps the lineage columns. Lineage is pure data ops (RBAC is S25's job). **26 lineage tests (15 build + 11 post-build review); 0 regressions.** Post-build review also hardened `resolve_live_set` to accept any lineage member id (normalised to root; unknown→None).
  - **Session 25 (Configurable Approval Workflow) COMPLETE (2026-06-29).** Generalised the Phase-2 single-reviewer Stage-4 sign-off into the configurable multi-level chain (§H.6 `src/governance/workflow.py`: `load_chain`/`required_final_level`/`next_required_level`/`check_segregation`/`record_signoff`/`reopen`/`pending_approvals` + `is_study_run_fit`), backed by the new append-only **hash-chained** `gold_governance_signoffs` log (§G.2) written via `src/governance/audit.py::append_event` (the §H.7 write half: `seq`/`prev_hash`/`entry_hash = sha256(canonical‖prev_hash)`). Extended formal approval to A/E **study runs** (FR-4-14), with materiality-driven required level (FR-4-16), absolute proposer≠approver + distinct-signer segregation (FR-4-05), attestation, a pending-approvals queue, and governed re-open (→ DRAFT child via `lineage.create_version`, original immutable). On a completing assumption-set APPROVE the set is locked and the legacy `gold_assumption_approvals` summary is still written; a single-`chief_actuary` chain reproduces legacy behaviour (NFR-G-08). **Full multi-level Stage-4 UI rebuild** (`ui/pages/23_tev_stage4.py`): authenticated `current_user()` actor (no free-text reviewer), chain-progress + pending-approvals panels, role-for-level/segregation pre-checks, attestation + comment + APPROVE/RETURN. **Owner Checkpoint 2 RESOLVED — proceed with documented `governance_config.yaml` defaults** (chain junior→senior→chief, `delta_tev_threshold: 0.01`, `final_level_below_threshold: senior_actuary`, default attestation). +38 governance tests (25 build + 13 post-build review); no `src/ai/` change. **Post-build review** fixed a latent hash-chain reproducibility landmine (tz-aware `signoff_ts` → normalise to naive UTC in `append_event` so a S26 `verify_chain` recompute matches), a materiality fallback (single-chief / role-not-in-chain), `reopen` (APPROVED-only guard + durable justification), and `pending_approvals` (include `STAGE3_APPROVED`). Detail in `docs/phase4_build_progress.md`.
  - **Session 26 (Audit Trail & Tamper-Evidence) COMPLETE (2026-07-01).** Closed FR-4-19…22 / NFR-G-04: new hash-chained **A/E governance-events** log (`gold_ae_governance_events`, §G.3) + the §G.5 additive `seq`/`prev_hash`/`entry_hash` columns on the two Phase-2 logs (schema-ready; the verifier begins each chain at the first hashed row). Completed the §H.7 **read/verify half** of `src/governance/audit.py`: `verify_chain` (recompute → `IntegrityResult` with `first_divergence_seq`; mirrors `append_event` byte-for-byte), `unified_audit_query` + `artifact_timeline` (one common event shape across the three physically-separate logs — sign-offs, A/E events, and the Phase-3 AI audit log via its §D.3 scheme; plus the legacy Phase-2 workflow/approval logs), and the A/E-event writers `record_ae_event`/`submit_study_run`. Two registries: a minimal `_HASH_CHAINED_TABLES` write allowlist (adds only the events log) + a superset `_VERIFIABLE_CHAINS` verify registry (covers the Phase-2 logs so they can be verified without opening a write path). **Owner choice "mechanism + natural hooks":** guarded STUDY_RUN_APPROVED/RETURNED emitted from `record_signoff` (study-run artifacts only) and DQ_OVERRIDE from the DQ dashboard UI action site — each wrapped so a failed emit can never break the primary action; the Phase-1/2 cores are untouched. New **Governance & Audit** Streamlit page (`ui/pages/26_governance_audit.py`, nav group added): filterable unified stream, per-artifact timeline, "Verify integrity" button. Added `IntegrityResult`/`AuditFilter` to `src/utils/types.py`. **+21 governance tests (16 build + 5 post-build review); no Phase 1–3 behaviour change.** Post-build adversarial review **found no defects** (all invariants empirically confirmed — clean/business-tamper/linkage-tamper/deleted-middle-row/empty/unhashed/float+tz-aware all correct); the +5 review tests lock the guarded natural-hook emits, the deleted-middle-row tamper, and the Phase-2 column-constant↔schema match. Detail in `docs/phase4_build_progress.md`.
  - **Session 27 (Governance Reporting, Tenancy-Readiness & Phase 4 UAT) COMPLETE (2026-07-01) — Phase 4 build CLOSED.** Realises FR-4-23…27 / NFR-G-06/07 + the §8.8 checklist. Added `src/governance/reporting.py` (§H.8: `dashboard_data` — states/live-set-per-lineage/**global** pending queue/recent activity; `export_compliance_pack` — APPROVED assumption set **or** fit study run → lineage + sign-offs/attestations + audit excerpt + per-change rationale + reproducibility + report links, rendered via the existing `autoescape=True` Jinja2 machinery into `src/reporting/templates/compliance_pack.html.j2`; **HTML now, `fmt='pdf'`→`NotImplementedError` per owner decision — no lockfile change**; non-approved→`ValueError`; `retention_policy` — no hard deletes), `src/governance/readiness.py` (§H.9 `check_tenancy_readiness` — AST identifier/import scan so docstrings never self-flag; returns `[]` on the shipped tree), and the **Governance Dashboard** page (`ui/pages/27_governance_dashboard.py`, nav group extended). Governance stays **outside `src/ai/`**, read-only parameterized DuckDB; **no schema change** (all tables exist), no Phase 1–3 behaviour change. Build fixes: a DuckDB **connection-overlap** bug (a held read-only handle silently emptied the rationale via `compare_versions`' own connection) → every reporting helper now owns a short-lived connection; `dashboard_data` made **tolerant of a pre-Phase-4 DB**; the live `data/experience_study.duckdb` (which predated all Phase-4 schema) was **migrated additively** via idempotent `init_database` (data preserved: 1 study run + 1 assumption set). **+51 tests** (reporting/readiness/regression/dashboard-AppTest, incl. **+18 post-build review**); AppTest render (unauth + authed via `current_user` patch) + a live Streamlit boot smoke both clean. Phase-4 UAT script authored (`docs/phase4_uat_script.md`, §8.8 + sign-off table). **Post-build review (2 adversarial agents + self-audit): no bypass/crash on the shipped tree; fixed the rationale `dimension` raw-dict render, a silent diff-failure masking, a submitted-but-unsigned study run missing from the pending queue, duplicate TEV links, orphan-root handling, and — highest value — **four `check_tenancy_readiness` scanner-evasion gaps** (import-alias/`getattr`/dict-key/`importlib` SSO `tenant_id`, plus crash-on-unparseable). Documented the effective-dating/live-set seam (chain approval doesn't set effective ranges).** Detail in `docs/phase4_build_progress.md`.
  - **Post-S27 governance UI + UAT hardening (2026-07-03/04).** During owner UAT: login-gate fix (`ui/pages/`→`ui/views/`); UAT Part 4/5 fixes (case-insensitive audit-id lookup; Stage-3 "Approve"→**"Submit for sign-off"**/`SUBMITTED_S4`; **`propose` enforced on TEV Stages 1–3**); and the **governance lifecycle UI** closing the engine-only seams — `ui/views/28_study_run_signoff.py` (study-run submit→sign-off→fit→compliance export) and `ui/views/29_assumption_lineage.py` (re-open, publish=effective-dating+supersede, compare), plus `ui/governance_logic.py`. Sidebar regrouped into **6 numbered workflow sections**; home page rebuilt with a graphical end-to-end flow. An end-of-session review (3 audit agents) completed Stage-3 propose-gating (envelope/report/refine) and made the five UAT harnesses (`scripts/uat_section{2,3_3,3_7,4_4,5_6}*.py`) non-destructive/robust (all pass). Engine unchanged; the two locked specs stay consistent (UI realises existing FRs). Detail in `docs/phase4_build_progress.md` → "End-of-session review (2026-07-04)".
  - **Governance-output audit + remediation (2026-07-04).** The owner ran the full governance workflow and uploaded the outputs (TEV iteration log, unified audit-stream exports, compliance-pack HTML) for a suspicious review. Figures verified sound; a code-level audit (6 adversarial agents, two rounds) found and fixed: **(1)** segregation-of-duties was silently non-functional for assumption sets because the TEV workflow captured a free-text `"ACTUARY_1"` proposer instead of `current_user()` (FR-4-03/05) — fixed across `ui/views/20-23_tev_stage*.py` **and** the DQ override (`02_data_quality.py`); **(2)** an APPROVED (locked) set could be silently unlocked via **both** `transition_assumption_set_status` **and** `save_assumption_set` (the Stage-2 editor downgrade) — both doors now guarded with `LockedStatusTransition` + Stage-2/3 UI locks; **(3)** compliance packs now warn when a set is APPROVED-but-not-effective (not live) and when the source study run isn't governance-approved (`compliance.require_fit_source_run` toggle, default warn); **(4)** the unified audit reader resolves free-text/username actors to one consistent display identity. **Migration note:** assumption sets created before this fix carry `author_id="ACTUARY_1"` and still fail-open for proposer≠approver — they must be remapped/re-proposed. No spec change (fixes realise existing FRs). Detail in `docs/phase4_build_progress.md` → "Governance-output audit + remediation".
  - **Full regression gate GREEN (2026-07-04): 1368 passed, 6 skipped, 0 failed** (S27 close was 1325; +20 from the post-S27 UI work, +23 from the governance-output audit above). The prod DB had been emptied before the S23 session (pre-existing, NOT Phase 4); the owner repopulated it via the Streamlit "Run Study", the AI models were fit and the TEV baseline/sensitivity state materialised programmatically, and in S27 the DB was migrated additively to carry the Phase-4 governance schema. Detail in `docs/phase4_build_progress.md`. **Owner UAT (`docs/phase4_uat_script.md`) closes Phase 4.**

## Tech stack
Python 3.11+ (the working environment is being upgraded from 3.9.6 to ≥3.10 ahead of Session 18 so the
mcp/FastMCP SDK can install — see progress doc; regenerate requirements.lock for the new interpreter), DuckDB, Pandas,
NumPy, Streamlit, Plotly, Jinja2, PyYAML, scipy, sqlglot (SQL boundary), pyliferisk (or
lifeActuary), pytest. Later Phase 3 sessions add: statsmodels, xgboost, shap, anthropic, openai,
mcp. (Data quality is custom rule-based validators, not Great Expectations.) Dependencies are
pinned in requirements.lock (compiled from requirements.in via uv); builds install from the
lockfile only.