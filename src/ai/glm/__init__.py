"""GLM assumption engine — fitting, factor derivation, bootstrap CIs (§7.4).

Populated in Session 15. Empty skeleton established in Session 14 so the
additive-layer architecture tests (FR-3A-07 import graph, FR-3A-09 write
contract, FR-3A-02 no-interpolation scan) hold from the outset.
"""
